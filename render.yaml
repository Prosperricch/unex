services:
  - type: web
    name: unex
    runtime: python
    buildCommand: pip install -r requirement.txt
    startCommand: gunicorn main:app --bind 0.0.0.0:10000 --workers 1 --timeout 120
    envVars:
      - key: PORT
        value: 10000
